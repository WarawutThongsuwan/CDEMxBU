using System.Collections;
using System.Collections.Generic;
using UnityEngine;
 
public enum TagType { A, B, C }
public enum NPCType { A, B, C }
public class TagController : MonoBehaviour
{
    public TagType tagType;
}